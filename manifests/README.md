# Manifests

This directory describes large datasets and heavy result archives that should
not be stored directly in the public GitHub repository.

A manifest should answer:

- what the dataset contains
- where it lives locally
- whether it has a public mirror
- how it relates to a minimal public example
- expected size

